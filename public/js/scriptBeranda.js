$("#dataTable").DataTable({
  searching: true, // hanya aktifkan pencarian
  lengthChange: true, // Menonaktifkan tombol panjang halaman
  responsive: true, // Responsif
  deferRender: true,
  scrollY: 300,
  scrollX: 400,
});
